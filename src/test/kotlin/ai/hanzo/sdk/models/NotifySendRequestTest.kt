/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.hanzo.sdk.models

import io.kotlintest.shouldBe
import io.kotlintest.specs.ShouldSpec

import ai.hanzo.sdk.models.NotifySendRequest
import ai.hanzo.sdk.models.NotifyChannel

class NotifySendRequestTest : ShouldSpec() {
    init {
        // uncomment below to create an instance of NotifySendRequest
        //val modelInstance = NotifySendRequest()

        // to test the property `to` - Destination address(es). Phone number for SMS, email for email. Multiple recipients fan out into one provider call each and produce one response entry per recipient. 
        should("test to") {
            // uncomment below to test the property
            //modelInstance.to shouldBe ("TODO")
        }

        // to test the property `channel`
        should("test channel") {
            // uncomment below to test the property
            //modelInstance.channel shouldBe ("TODO")
        }

        // to test the property `provider` - Pins a specific provider service name (e.g. `twilio`, `plivo`, `twilio_email`, `mail`). When omitted the fold picks the org's default for the channel from configured KMS credentials. 
        should("test provider") {
            // uncomment below to test the property
            //modelInstance.provider shouldBe ("TODO")
        }

        // to test the property `subject` - Raw subject; used verbatim when `body` is set (no-template path).
        should("test subject") {
            // uncomment below to test the property
            //modelInstance.subject shouldBe ("TODO")
        }

        // to test the property `body` - Raw message body. When non-empty it wins verbatim and no template is rendered. 
        should("test body") {
            // uncomment below to test the property
            //modelInstance.body shouldBe ("TODO")
        }

        // to test the property `templateId` - Built-in template identifier. When `body` is empty, the template is resolved from `template_id` or, failing that, `event`. 
        should("test templateId") {
            // uncomment below to test the property
            //modelInstance.templateId shouldBe ("TODO")
        }

        // to test the property `templateVars` - Free-form variables the template is rendered against (e.g. `otp`, `app`, `recipient`). A missing `app` defaults to `Hanzo`. 
        should("test templateVars") {
            // uncomment below to test the property
            //modelInstance.templateVars shouldBe ("TODO")
        }

        // to test the property `event` - Event-catalog identifier. Also used as a fallback template id when `template_id` is unset (the IAM OTP path sends `event: iam.otp_sent` with no `template_id`). 
        should("test event") {
            // uncomment below to test the property
            //modelInstance.event shouldBe ("TODO")
        }

        // to test the property `idempotencyKey` - Per-tenant deduplication key. Accepted on the wire; the sync fold does not persist a Message row. 
        should("test idempotencyKey") {
            // uncomment below to test the property
            //modelInstance.idempotencyKey shouldBe ("TODO")
        }

        // to test the property `sendAt` - RFC3339 schedule time for async dispatch. Async is not available in the fold, so a set value has no effect on the sync path. 
        should("test sendAt") {
            // uncomment below to test the property
            //modelInstance.sendAt shouldBe ("TODO")
        }

        // to test the property `options` - Free-form per-provider knob bag.
        should("test options") {
            // uncomment below to test the property
            //modelInstance.options shouldBe ("TODO")
        }

    }
}
