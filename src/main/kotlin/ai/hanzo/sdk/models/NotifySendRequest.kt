/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.hanzo.sdk.models

import ai.hanzo.sdk.models.NotifyChannel

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * Notification send request (`notify/pkg/types.SendRequest`). Delivery content is either a raw `subject`/`body`, or a template resolved from `template_id`/`event` rendered against `template_vars`. 
 *
 * @param to Destination address(es). Phone number for SMS, email for email. Multiple recipients fan out into one provider call each and produce one response entry per recipient. 
 * @param channel 
 * @param provider Pins a specific provider service name (e.g. `twilio`, `plivo`, `twilio_email`, `mail`). When omitted the fold picks the org's default for the channel from configured KMS credentials. 
 * @param subject Raw subject; used verbatim when `body` is set (no-template path).
 * @param body Raw message body. When non-empty it wins verbatim and no template is rendered. 
 * @param templateId Built-in template identifier. When `body` is empty, the template is resolved from `template_id` or, failing that, `event`. 
 * @param templateVars Free-form variables the template is rendered against (e.g. `otp`, `app`, `recipient`). A missing `app` defaults to `Hanzo`. 
 * @param event Event-catalog identifier. Also used as a fallback template id when `template_id` is unset (the IAM OTP path sends `event: iam.otp_sent` with no `template_id`). 
 * @param idempotencyKey Per-tenant deduplication key. Accepted on the wire; the sync fold does not persist a Message row. 
 * @param sendAt RFC3339 schedule time for async dispatch. Async is not available in the fold, so a set value has no effect on the sync path. 
 * @param options Free-form per-provider knob bag.
 */


data class NotifySendRequest (

    /* Destination address(es). Phone number for SMS, email for email. Multiple recipients fan out into one provider call each and produce one response entry per recipient.  */
    @Json(name = "to")
    val to: kotlin.collections.List<kotlin.String>,

    @Json(name = "channel")
    val channel: NotifyChannel? = null,

    /* Pins a specific provider service name (e.g. `twilio`, `plivo`, `twilio_email`, `mail`). When omitted the fold picks the org's default for the channel from configured KMS credentials.  */
    @Json(name = "provider")
    val provider: kotlin.String? = null,

    /* Raw subject; used verbatim when `body` is set (no-template path). */
    @Json(name = "subject")
    val subject: kotlin.String? = null,

    /* Raw message body. When non-empty it wins verbatim and no template is rendered.  */
    @Json(name = "body")
    val body: kotlin.String? = null,

    /* Built-in template identifier. When `body` is empty, the template is resolved from `template_id` or, failing that, `event`.  */
    @Json(name = "template_id")
    val templateId: kotlin.String? = null,

    /* Free-form variables the template is rendered against (e.g. `otp`, `app`, `recipient`). A missing `app` defaults to `Hanzo`.  */
    @Json(name = "template_vars")
    val templateVars: kotlin.collections.Map<kotlin.String, kotlin.Any>? = null,

    /* Event-catalog identifier. Also used as a fallback template id when `template_id` is unset (the IAM OTP path sends `event: iam.otp_sent` with no `template_id`).  */
    @Json(name = "event")
    val event: kotlin.String? = null,

    /* Per-tenant deduplication key. Accepted on the wire; the sync fold does not persist a Message row.  */
    @Json(name = "idempotency_key")
    val idempotencyKey: kotlin.String? = null,

    /* RFC3339 schedule time for async dispatch. Async is not available in the fold, so a set value has no effect on the sync path.  */
    @Json(name = "send_at")
    val sendAt: kotlin.String? = null,

    /* Free-form per-provider knob bag. */
    @Json(name = "options")
    val options: kotlin.collections.Map<kotlin.String, kotlin.Any>? = null

) {


}

