/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.hanzo.sdk.models


import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * JetStream consumer configuration.
 *
 * @param durableName Durable consumer name. If set, the consumer survives client disconnections. 
 * @param filterSubject Subject filter. Only messages matching this subject are delivered. Supports wildcards. 
 * @param ackPolicy Acknowledgment policy. `explicit` requires per-message ack. `all` acks all messages up to the acked sequence. `none` disables acks. 
 * @param deliverPolicy Where to start delivery. `all` delivers from the beginning. `last` delivers the last message. `new` delivers only new messages. `by_start_sequence` and `by_start_time` start from a specific point. 
 * @param optStartSeq Starting sequence number (used with deliver_policy `by_start_sequence`). 
 * @param optStartTime Starting timestamp (used with deliver_policy `by_start_time`). 
 * @param maxDeliver Maximum delivery attempts before the message is dropped or sent to a dead letter subject (-1 for unlimited). 
 * @param ackWait Time to wait for acknowledgment before redelivery (e.g., \"30s\", \"5m\"). Defaults to \"30s\". 
 * @param replayPolicy Replay policy for historical messages. `instant` delivers as fast as possible. `original` preserves original timing gaps. 
 * @param maxAckPending Maximum number of unacknowledged messages before delivery pauses. 
 * @param description Optional human-readable description.
 */


data class MqConsumerConfig (

    /* Durable consumer name. If set, the consumer survives client disconnections.  */
    @Json(name = "durable_name")
    val durableName: kotlin.String? = null,

    /* Subject filter. Only messages matching this subject are delivered. Supports wildcards.  */
    @Json(name = "filter_subject")
    val filterSubject: kotlin.String? = null,

    /* Acknowledgment policy. `explicit` requires per-message ack. `all` acks all messages up to the acked sequence. `none` disables acks.  */
    @Json(name = "ack_policy")
    val ackPolicy: MqConsumerConfig.AckPolicy? = AckPolicy.explicit,

    /* Where to start delivery. `all` delivers from the beginning. `last` delivers the last message. `new` delivers only new messages. `by_start_sequence` and `by_start_time` start from a specific point.  */
    @Json(name = "deliver_policy")
    val deliverPolicy: MqConsumerConfig.DeliverPolicy? = DeliverPolicy.all,

    /* Starting sequence number (used with deliver_policy `by_start_sequence`).  */
    @Json(name = "opt_start_seq")
    val optStartSeq: kotlin.Int? = null,

    /* Starting timestamp (used with deliver_policy `by_start_time`).  */
    @Json(name = "opt_start_time")
    val optStartTime: java.time.OffsetDateTime? = null,

    /* Maximum delivery attempts before the message is dropped or sent to a dead letter subject (-1 for unlimited).  */
    @Json(name = "max_deliver")
    val maxDeliver: kotlin.Int? = -1,

    /* Time to wait for acknowledgment before redelivery (e.g., \"30s\", \"5m\"). Defaults to \"30s\".  */
    @Json(name = "ack_wait")
    val ackWait: kotlin.String? = "30s",

    /* Replay policy for historical messages. `instant` delivers as fast as possible. `original` preserves original timing gaps.  */
    @Json(name = "replay_policy")
    val replayPolicy: MqConsumerConfig.ReplayPolicy? = ReplayPolicy.instant,

    /* Maximum number of unacknowledged messages before delivery pauses.  */
    @Json(name = "max_ack_pending")
    val maxAckPending: kotlin.Int? = 1000,

    /* Optional human-readable description. */
    @Json(name = "description")
    val description: kotlin.String? = null

) {

    /**
     * Acknowledgment policy. `explicit` requires per-message ack. `all` acks all messages up to the acked sequence. `none` disables acks. 
     *
     * Values: none,all,explicit
     */
    @JsonClass(generateAdapter = false)
    enum class AckPolicy(val value: kotlin.String) {
        @Json(name = "none") none("none"),
        @Json(name = "all") all("all"),
        @Json(name = "explicit") explicit("explicit");
    }
    /**
     * Where to start delivery. `all` delivers from the beginning. `last` delivers the last message. `new` delivers only new messages. `by_start_sequence` and `by_start_time` start from a specific point. 
     *
     * Values: all,last,new,by_start_sequence,by_start_time,last_per_subject
     */
    @JsonClass(generateAdapter = false)
    enum class DeliverPolicy(val value: kotlin.String) {
        @Json(name = "all") all("all"),
        @Json(name = "last") last("last"),
        @Json(name = "new") new("new"),
        @Json(name = "by_start_sequence") by_start_sequence("by_start_sequence"),
        @Json(name = "by_start_time") by_start_time("by_start_time"),
        @Json(name = "last_per_subject") last_per_subject("last_per_subject");
    }
    /**
     * Replay policy for historical messages. `instant` delivers as fast as possible. `original` preserves original timing gaps. 
     *
     * Values: instant,original
     */
    @JsonClass(generateAdapter = false)
    enum class ReplayPolicy(val value: kotlin.String) {
        @Json(name = "instant") instant("instant"),
        @Json(name = "original") original("original");
    }

}

